# Shell scripts must always use LF line endings (required for Linux/Docker execution)
*.sh text eol=lf

# PowerShell scripts use CRLF on Windows
*.ps1 text eol=crlf

# Default: auto line ending for text files
* text=auto
